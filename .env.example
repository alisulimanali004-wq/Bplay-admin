# Copy to .env.local and adjust. Base URL of the bplay-backend API.
VITE_API_BASE_URL=http://localhost:3000/api/v1
# "true" = run on in-memory mock data; "false" = hit the real backend.
VITE_USE_MOCKS=true
