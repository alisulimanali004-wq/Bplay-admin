# Standalone DEMO build for sharing a live link.
# Runs 100% on in-memory mock data — needs no backend, no server, no database.
# Build it with:  npm run build:demo
VITE_USE_MOCKS=true
VITE_API_BASE_URL=/api/v1
